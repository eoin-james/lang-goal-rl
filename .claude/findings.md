# Findings & Tech Debt

- [ ] **Portfolio close-out audit, 2026-07-30 (blocking):** Phase 2a is narrated in `BLOG.md` as finished and work has moved into Stage 11, but the portfolio contract has not been satisfied. Stage 7 still awaits human sign-off; `README.md`/`STATUS.md` remain frozen at Phase 1; the Stage 10 demo lacks exact checkpoint/seed, regeneration command, and explicit success verification; there is no top-level clean-checkout reproduction interface, environment/runtime contract, CI, provenance manifest, or release snapshot; new Stage 10 JSON repeats absolute local checkpoint paths; and completed blog/demo work was not reconciled in this findings ledger. Before calling Phase 2a portfolio-ready, resolve or explicitly disposition every applicable open item below, make all public status documents agree, and record closure evidence beside each resolved finding. Continuing later experiments is allowed, but must not be presented as proof that this phase-level close-out happened.
- [ ] `src/lang_goal_rl/midepisode_regoal.py:149` — `rollout_with_goal_switch` only tracks the *final* step's `is_success`, not the *first* post-switch success step. Stage 6's time-to-redirect metric needed the latter, so `experiments/06_live_english_interface/live_regoal_eval.py` carries a local, instrumented duplicate (`rollout_with_goal_switch_timed`) that otherwise mirrors the reusable function exactly. Consider promoting an optional per-step trace / first-success-step return value into `midepisode_regoal.py` if a future stage needs the same timing data, to avoid a second duplicate.
- [ ] `src/lang_goal_rl/interactive_demo.py` — the background stdin-reader thread dies silently on EOF (e.g. a closed/reopened FIFO) with no error message; the main episode loop then keeps running the last instruction forever with no way to quit via typed input, only `kill`. Also: `_build_renderer`'s matplotlib headless fallback has zero test coverage and was never actually exercised live — on macOS, `render_mode="human"` succeeds even with `$DISPLAY` unset (that check is X11-flavored and doesn't apply), so nobody has confirmed the fallback branch actually renders correctly when it does trigger (e.g. on a genuinely headless Linux box). Found during an end-to-end smoke test on 2026-07-28, not by the unit tests.
- [x] `experiments/09_waypoint_following/` used only a single checkpoint (`01_uvfa_her_baseline/checkpoints/seed_0.zip`), not the tiered multi-seed convention. Resolved on 2026-07-28 by rerunning the protocol across all 8 healthy checkpoints, completing review, and recording the PASS in commit `6bf4dfe`.
- [ ] `README.md`, `STATUS.md`, `PHASE2_ROADMAP.md` — No consistent front-page route exists through current Phase 2a work. `STATUS.md` and the README's primary status link stop at Stage 6 while Phase 2a has completed Stages 8-9 and is building Stage 10. Make the current phase and links to its roadmap, reports, demos, and reproduction commands unambiguous.
- [ ] `PHASE2_ROADMAP.md:16`, `experiments/07_frame_of_reference_check/` — Stage 7 is listed as "Not started" even though all six clips and their report exist; the actual state is awaiting human sign-off. Record that state accurately, obtain a dated sign-off for each direction label, and only then mark the stage done.
- [ ] `BLOG.md`, `demos/README.md` — Portfolio narrative and curated demos stop at Phase 1/Stage 6. Before Phase 2a close-out, add a dated phase narrative covering progression, failures, evidence, limitations, and links, plus a provenance-documented final demo and regeneration command.
- [ ] `BLOG.md:204`, `demos/README.md:268`, `experiments/06_live_english_interface/make_demo.py` — Public wording implies the reported Stage 6 metrics and GIF came from a person typing instructions, but both are produced by scripted live-loop harnesses. Label measured artifacts as scripted real-time evaluation; either add a separately labeled terminal-interaction capture or remove wording that implies human input produced the reported numbers.
- [ ] `experiments/README.md`, experiment scripts — No top-level "reproduce stage N" interface exists. Add and clean-checkout-test one command per stage for evaluation from committed artifacts and one for demo regeneration; distinguish these from full retraining.
- [ ] `README.md`, `pyproject.toml` — Clean-checkout prerequisites omit platform and MuJoCo requirements, model-download/offline behavior, hardware, runtime, disk use, and artifact provenance. Document and validate a concise environment contract.
- [ ] `.github/`, project root — No CI verifies tests and quality gates. A 2026-07-28 oversight run reached 19 passes then 7 failures before hanging during model/cache access. Add deterministic unit-test CI, separate external-model integration checks, and record clean-environment commands that pass.
- [ ] `experiments/09_waypoint_following/runs/final_results.json:3`, `experiments/09_waypoint_following/runs/tier1_results.json:3` — Committed JSON contains `/Users/eoinmca/Projects/lang-goal-rl/...`. Store repository-relative artifact identifiers and audit all committed reports, JSON, and logs for local absolute paths.
- [ ] `src/lang_goal_rl/language_embedding.py:28`, experiment evidence — The Hugging Face sentence model is loaded by mutable alias without a pinned repository revision, and stage evidence is not bound to source/checkpoint/output hashes. Pin the model revision and add a provenance manifest containing the git revision, lockfile hash, checkpoint hashes, and hashes of reported JSON/charts/demos.
- [ ] `.claude/agents/CONTRACTS.md`, `.claude/agents/experiment-runner.md` — Seed policy is inconsistent: the shared contract requires tiered 3-then-10 runs, while the runner instructions still say to launch 5 seeds. Establish one enforceable policy before reviewing further experiments.
- [ ] Repository releases — Completed phases have no versioned tag/release freezing code, artifacts, results, demo, narrative, and known limitations. Create a Phase 1 release snapshot and require one at future phase close-out.
- [ ] Repository artifact policy — The checkout is approximately 1.4 GB and tracks many checkpoints and generated assets without identifying canonical versus regenerable artifacts. Document retention, expected checkout/cache sizes, and how portfolio-critical results remain accessible.
- [ ] `src/lang_goal_rl/command_type_vocabulary.py` — Stage 11's `check_cross_class_embedding_overlap` diagnostic only covers the MOVE/GOTO_NAMED_REGION pair it was built to catch. Attempt 3 found a smaller, bounded RESET->STOP collision (two idiomatic phrasings, "kick things off again" and "wipe the slate clean") that this diagnostic doesn't flag. Extend it to check all class pairs, not just the one it was built for, so future vocabulary edits catch this category of issue before a full train+eval cycle. Found while closing stage 11 (2026-07-30), reviewer-confirmed non-blocking for that stage's own gate.
