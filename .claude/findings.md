# Findings & Tech Debt

- [ ] `src/lang_goal_rl/midepisode_regoal.py:149` — `rollout_with_goal_switch` only tracks the *final* step's `is_success`, not the *first* post-switch success step. Stage 6's time-to-redirect metric needed the latter, so `experiments/06_live_english_interface/live_regoal_eval.py` carries a local, instrumented duplicate (`rollout_with_goal_switch_timed`) that otherwise mirrors the reusable function exactly. Consider promoting an optional per-step trace / first-success-step return value into `midepisode_regoal.py` if a future stage needs the same timing data, to avoid a second duplicate.
- [ ] Repo hygiene/presentation gaps flagged by a deleted `NEXT_PHASES.md` draft, not yet actioned: no single "reproduce stage N" command documented anywhere (a reader has to piece together checkpoint paths + script names per experiment); no documented runtime/hardware/checkpoint requirements; no CI config; no changelog or versioned release; unconfirmed whether any committed log/report references absolute local paths that would break for another reader (a prior cleanup pass fixed `report.md` image/log links, but full logs themselves weren't audited for this). Worth a dedicated hygiene pass before treating the repo as "presentable" per the portfolio-project direction (see project memory `lang-goal-rl-portfolio-direction`).
