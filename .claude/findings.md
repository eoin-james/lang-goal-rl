# Findings & Tech Debt

- [ ] **Portfolio close-out audit, 2026-07-30 (blocking) — updated 2026-07-30, partially resolved.** A cleanup pass resolved every automatable item this finding named: front-page routing, stage-7 label accuracy, environment/runtime contract, CI, reproduction commands for every stage, absolute-path hygiene, model revision pinning (partial — see its own item below), seed-policy consistency, and an artifact-retention policy note — see each item's own `[x]` entry below for evidence. **Still genuinely open, not closeable by an agent:** (1) Stage 7's human visual sign-off itself hasn't happened yet — only its status *label* is now accurate; (2) the `phase2a-portfolio-ready` tag is deliberately not created until (1) lands; (3) the full provenance-manifest (per-artifact hashes bound to git revision) was descoped as bigger than this pass, tracked as its own forward-looking item; (4) three findings below (`midepisode_regoal.py:149`, `interactive_demo.py`'s stdin/matplotlib gaps, `check_cross_class_embedding_overlap`'s single-pair coverage) need real code-behavior decisions, not cleanup, and were deliberately left untouched by this pass. This item stays open until all four close.
- [ ] `src/lang_goal_rl/midepisode_regoal.py:149` — `rollout_with_goal_switch` only tracks the *final* step's `is_success`, not the *first* post-switch success step. Stage 6's time-to-redirect metric needed the latter, so `experiments/06_live_english_interface/live_regoal_eval.py` carries a local, instrumented duplicate (`rollout_with_goal_switch_timed`) that otherwise mirrors the reusable function exactly. Consider promoting an optional per-step trace / first-success-step return value into `midepisode_regoal.py` if a future stage needs the same timing data, to avoid a second duplicate. **Deliberately not touched by the 2026-07-30 cleanup pass — needs an API design decision, not a mechanical fix.**
- [ ] `src/lang_goal_rl/interactive_demo.py` — the background stdin-reader thread dies silently on EOF (e.g. a closed/reopened FIFO) with no error message; the main episode loop then keeps running the last instruction forever with no way to quit via typed input, only `kill`. Also: `_build_renderer`'s matplotlib headless fallback has zero test coverage and was never actually exercised live — on macOS, `render_mode="human"` succeeds even with `$DISPLAY` unset (that check is X11-flavored and doesn't apply), so nobody has confirmed the fallback branch actually renders correctly when it does trigger (e.g. on a genuinely headless Linux box). Found during an end-to-end smoke test on 2026-07-28, not by the unit tests. **Deliberately not touched by the 2026-07-30 cleanup pass — needs a real bugfix plus new test coverage, not a mechanical fix.**
- [x] `experiments/09_waypoint_following/` used only a single checkpoint (`01_uvfa_her_baseline/checkpoints/seed_0.zip`), not the tiered multi-seed convention. Resolved on 2026-07-28 by rerunning the protocol across all 8 healthy checkpoints, completing review, and recording the PASS in commit `6bf4dfe`.
- [x] `README.md`, `STATUS.md`, `PHASE2_ROADMAP.md` — No consistent front-page route existed through Phase 2a/2b work. Resolved 2026-07-30: `README.md` now links `STATUS.md`/`PHASE2_ROADMAP.md`/`PHASE2B_ROADMAP.md` alongside `ROADMAP.md`/`PHASES.md` and its framing line reflects Phase 2b (not "This is Phase 1"); `STATUS.md` gained a full `## Phase 2b` section mirroring the existing Phase 2a section.
- [x] `PHASE2_ROADMAP.md:16`, `experiments/07_frame_of_reference_check/` — Stage 7's Status label was wrong ("Not started" despite all six clips + report existing). Resolved 2026-07-30: label now reads "Awaiting human sign-off," accurately reflecting reality. **The underlying sign-off itself is still pending** — that part of this finding stays open until the user actually reviews the 6 clips; only the label was fixed, not the substance.
- [x] `BLOG.md`, `demos/README.md` — Portfolio narrative and curated demos stopped at Phase 1/Stage 6. Resolved: `BLOG.md` has dated Phase 2a (2026-07-28) and Phase 2b (2026-07-30) entries; `demos/README.md` has item 9 (Stage 10 capstone, with checkpoint/seed/regeneration detail and success verification).
- [x] `BLOG.md:204`, `demos/README.md:268`, `experiments/06_live_english_interface/make_demo.py` — Stage 6 wording implied a human typed instructions live. Resolved 2026-07-30: both `BLOG.md`'s Stage 6 entry and `demos/README.md` item 8 now carry a disclaimer stating the reported numbers come from `live_regoal_eval.py`'s scripted harness, not live human typing.
- [x] `experiments/README.md`, experiment scripts — No top-level "reproduce stage N" interface existed. Resolved 2026-07-30: every `experiments/NN_slug/evidence.md` (00 through 11) now has a `### Reproduce` section with a real, personally-executed command (or numbered launch-then-aggregate sequence), each verified against that stage's own already-reported numbers — exact or byte-for-byte match in every case, no discrepancies found. Stage 07 (whose gate is human sign-off, not a metric) documents a clip-regeneration command instead of a metrics command. `experiments/README.md` states the general convention once.
- [x] `README.md`, `pyproject.toml` — No environment contract existed. Resolved 2026-07-30: `README.md` gained an `## Environment contract` section (OS, Python version, MuJoCo/`gymnasium-robotics` notes including the historical `pybullet` build-failure trap, HF network/offline behavior, test runtime, disk-use pointer).
- [x] `.github/`, project root — No CI existed. Resolved 2026-07-30: `.github/workflows/tests.yml` added (`uv sync` + `uv run pytest tests/lang_goal_rl -q` on push/PR, `ubuntu-latest` — confirmed safe since no test uses `render_mode="human"`). **Not yet pushed to `origin`** — authored and validated locally, pending a separate explicit push decision (visible/public action, flagged deliberately rather than pushed automatically).
- [x] `experiments/09_waypoint_following/runs/final_results.json:3`, `experiments/09_waypoint_following/runs/tier1_results.json:3` — Committed JSON contained absolute local paths. Resolved 2026-07-30, and scope widened beyond what this finding named: all 17 affected files across both `experiments/09_waypoint_following/runs/` (9 files) and `experiments/10_typed_command_interface/runs/` (8 files) now store repo-relative `"checkpoint"` paths, confirmed via repo-wide grep returning zero remaining matches in JSON. Raw `.log`/`stdout.log` files are deliberately left untouched (documented in `experiments/README.md` as expected point-in-time console captures, not portability-broken).
- [x] `src/lang_goal_rl/language_embedding.py:28`, experiment evidence — **Partially resolved 2026-07-30.** The model-pinning half is done: `DEFAULT_MODEL_REVISION = "1110a243fdf4706b3f48f1d95db1a4f5529b4d41"` added and passed into `SentenceTransformer(...)`, cross-checked via both the live HuggingFace Hub API and the local cache snapshot directory (agreed exactly), verified via a bit-identical embedding sanity check and the full test suite. **The full provenance-manifest half (git revision + lockfile hash + checkpoint hashes + report/chart/demo hashes) is explicitly NOT built** — descoped as materially bigger than a revision pin; tracked here as a distinct, still-open forward-looking item, not silently dropped.
- [x] `.claude/agents/CONTRACTS.md`, `.claude/agents/experiment-runner.md` — Seed policy was inconsistent (tiered 3-then-10 vs. a leftover "launch 5 seeds" line). Resolved 2026-07-30: `experiment-runner.md`'s "Working shape" section now matches its own stated tiered convention.
- [x] Repository releases — No versioned tag/release existed. Resolved 2026-07-30 for Phase 1: `phase1-complete` tag created locally at commit `98e159f` ("feat: reframe as Phase 1, merge interactive demo scripts, reconcile phase docs"). **Not yet pushed** (visible/public action, deliberately held for a separate explicit decision). A `phase2a-portfolio-ready` tag is explicitly deferred until Stage 7's sign-off lands — not tagged early and back-filled.
- [x] Repository artifact policy — No canonical-vs-regenerable documentation existed. Resolved 2026-07-30: `experiments/README.md` gained an artifact-policy section with fresh measured sizes (repo 1.4GB total, dominated by gitignored `.venv`; `experiments/` itself 88MB, 79 committed binaries — 13 `.zip`, 5 `.pt`, 6 `.gif`, 55 `.png`), the canonical (checkpoints)/regenerable (charts/logs/demos) split, and why no Git LFS yet (size doesn't justify it at this scale).
- [ ] `src/lang_goal_rl/command_type_vocabulary.py` — Stage 11's `check_cross_class_embedding_overlap` diagnostic only covers the MOVE/GOTO_NAMED_REGION pair it was built to catch. Attempt 3 found a smaller, bounded RESET->STOP collision (two idiomatic phrasings, "kick things off again" and "wipe the slate clean") that this diagnostic doesn't flag. Extend it to check all class pairs, not just the one it was built for, so future vocabulary edits catch this category of issue before a full train+eval cycle. Found while closing stage 11 (2026-07-30), reviewer-confirmed non-blocking for that stage's own gate. **Deliberately not touched by the 2026-07-30 cleanup pass — needs a generalized check design, not a mechanical fix.**
